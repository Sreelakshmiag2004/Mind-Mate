# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.12,<1.0    # required by FastAPI for File()/Form() multipart parsing (media uploads)

# --- Database ---
sqlalchemy>=2.0,<2.1
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0

# --- Settings / validation ---
pydantic>=2.9,<3.0
pydantic-settings>=2.6,<3.0
email-validator>=2.2,<3.0

# --- Auth / security ---
passlib[argon2]>=1.7,<2.0
argon2-cffi>=23.1
PyJWT>=2.9,<3.0
python-dotenv>=1.0,<2.0

# --- Object storage (Phase 3) ---
boto3>=1.35,<2.0    # S3-compatible client — talks to MinIO locally and real AWS S3 in production, unchanged

# --- AI / weekly reflection (Phase 5) ---
anthropic>=0.69,<1.0    # official Claude SDK — see app/services/ai/anthropic_provider.py

# --- Testing ---
pytest>=8.3,<9.0
httpx>=0.27,<1.0
# No pytest-asyncio needed: reflections' async routes/service are exercised
# entirely through the synchronous TestClient (Starlette drives the ASGI
# app's event loop internally), exactly like media.py's async upload route
# already is in tests/test_media.py — see tests/test_reflections.py.
